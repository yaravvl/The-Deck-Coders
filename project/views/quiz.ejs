<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=title%></title>
    <link rel="stylesheet" href="../css/reset.css" />
    <link rel="stylesheet" href="../css/style.css" />
    <link rel="stylesheet" href="../css/quiz.css" />
    <link rel="stylesheet" href="../css/dark-mode.css" />
    <link
      href="https://fonts.googleapis.com/css2?family=Oswald&family=Qwitcher+Grypen:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css"
    />
    <script src="javascript/profile.js"></script>
    <link rel="icon" type="image/jpg" href="/assets/images/favicon.jpg">
    <script>
      function showLockPopup(event) {
        if (event.currentTarget.classList.contains('locked')) {
          event.preventDefault();
          document.getElementById('lockPopup').classList.add('show');
          document.getElementById('popupOverlay').classList.add('show');
        }
      }

      function closeLockPopup() {
        document.getElementById('lockPopup').classList.remove('show');
        document.getElementById('popupOverlay').classList.remove('show');
      }
    </script>
  </head>
  <body>
    <%- include("partials/nav") %>
    <main>
      <section class="quiz-pagina">
        <article id="article-one">
          <a href="/quiz/10-rounds">
            <h2>10 Rounds</h2>
            <p>
              Bij deze quiz krijg je 10 quotes waarbij je de juiste combinatie
              van karakter en quote moet aanduiden.
            </p></a
          >
        </article>

        <article id="article-two">
          <a href="/quiz/sudden-death">
            <h2>Sudden Death</h2>
            <p>
              Bij deze quiz blijf je quotes krijgen om de juiste combinatie van
              karakter en film aan te duiden tot je een fout antwoord geeft.
            </p></a
          >
        </article>

        <article id="article-three" class="<%= user.level < 3 ? 'locked' : '' %>" onclick="showLockPopup(event)">
          <a href="/quiz/timed-quiz">
            <h2>Timed quiz</h2>
            <p>
              Bij deze quiz moet je zo veel mogelijk combinaties tussen karakter
              en film aanduiden binnen de tijd.
            </p></a
          >
        </article>
      </section>
    </main>

    <!-- Popup and overlay -->
    <div id="popupOverlay" class="popup-overlay" onclick="closeLockPopup()"></div>
    <div id="lockPopup" class="popup">
      <h3>Vergrendelde inhoud</h3>
      <p>Je moet level 3 bereiken om de Timed Quiz te ontgrendelen.</p>
      <p>Huidig level: <%= user.level %></p>
      <button onclick="closeLockPopup()">Sluiten</button>
    </div>

    <%- include("partials/footer") %>
  </body>
</html>
